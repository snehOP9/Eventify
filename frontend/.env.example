VITE_API_BASE_URL=/api
VITE_LOCAL_BACKEND_ORIGIN=http://localhost:8080
# Optional for deployed frontends when OAuth/browser redirects should target a
# dedicated backend host. Leave blank if your deployment already proxies /api.
VITE_BROWSER_BACKEND_ORIGIN=
VITE_USE_MOCK_API=false
